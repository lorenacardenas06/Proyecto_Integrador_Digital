<!DOCTYPE html>
<html lang="en">
<head>
    <title>Perfil de usuario</title>
    <link rel="stylesheet" href="/css/perfil.css"></link>
    <%-include("../partials/head")%>
    <%-include("../partials/header")%>
</head>
<body class="body">
  <main>
    <div class="container">
        <div class="main-body">
          <div class="row gutters-sm">
            <div class="col-md-4 mb-3">
              <div class="card">
                <div class="card-body">
                  <div class="d-flex flex-column align-items-center text-center">
                    <img src="<%=usuario.imagen%>" alt="Imagen de usuario" class="rounded-circle" width="150">
                    <div class="mt-3">
                      <h4><%=usuario.nombre%> <%=usuario.apellido%></h4>
                      <p class="text-muted font-size-sm"><%=usuario.rol%></p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <div class="col-md-8">
              <form action="/user/editarPerfil/<%= usuario.id%>?_method=PUT" method="POST" enctype="multipart/form-data">
                <div class="card mb-3">
                  <div class="card-body">
                    <div class="row">
                      <div class="col-sm-3">
                        <h6 class="mb-0">Nombre:</h6>
                      </div>
                      <div class="col-sm-9 text-secondary">
                        <!--same as in registro-->
                        <input type="text" id="nombre" name="nombre" class="form-input required" minlength="2" maxlength="20" pattern="[A-Za-z ]{2,20}" title="El nombre debe tener entre 2 y 20 caracteres y solo puede contener letras" required>
                        
                      </div>
                    </div>
                    <hr>
                    <div class="row">
                      <div class="col-sm-3">
                        <h6 class="mb-0">Apellidos</h6>
                      </div>
                      <div class="col-sm-9 text-secondary">
                        <input type="text" id="apellido" name="apellido" class="form-input" minlength="2" maxlength="20" pattern="[A-Za-z ]{2,20}" title="El apellido debe tener entre 2 y 20 caracteres y solo puede contener letras" required>
                      </div>
                    </div>
                    <hr>
                    <div class="row">
                      <div class="col-sm-3">
                        <h6 class="mb-0">Email</h6>
                      </div>
                      <div class="col-sm-9 text-secondary">
                        <%=usuario.email%>
                      </div>
                    </div>
                    <hr>
                    <div class="row">
                      <div class="col-sm-3">
                        <h6 class="mb-0">Cargue nueva imagen:</h6>
                      </div>
                      <div class="col-sm-9 text-secondary">
                        <input type="file" name="imagen" id="imagen" multiple accept="image/jpeg, image/jpg, image/png, image/gif" /></input>
                      </div>
                    </div>
                    <hr>
                    <div class="row">
                      <div class="col-sm-12">
                        <button type="submit" class="boton">Actualizar datos</button>
                      </div>
                    </div>
                  </div>
                </div>
              </form>
            </div>
  </main>
  <%-include("../partials/footer")%>
</body>
</html>